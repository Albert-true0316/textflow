# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 上手必读

请先逐项打勾下方任务，表示已知晓操作方式。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

---

# TextFlow 待办

手动维护或交给 App / AI 操作。本文件是唯一真相源。

## 快捷键（任务行获得焦点时）

先用 **Tab** 在任务间移动焦点，或 **点击任务文字** 直接编辑。

| 按键                       | 作用                    |
| ------------------------ | --------------------- |
| **Enter**                | 编辑名称                  |
| **Tab**                  | 在此下新增一步（类似 Word 列表缩进） |
| **⌘ / Ctrl + Enter**     | 勾选 / 取消完成             |
| **⌘ / Ctrl + Shift + D** | AI 拆解                 |
| **Backspace / Delete**   | 删除该任务                 |

编辑名称时：**Enter** 保存，**Tab** 保存并在此下新增一步，**Esc** 取消。

底部输入框：**Enter** 添加顶层任务（有 API Key 时默认识别自然语言）。

---

- [ ] 买菜 🗓️2026-07-15 #生活 ^a3f2
  - [ ] 列出本周要买的菜品清单 #生活 ^dvft
  - [ ] 查看冰箱库存避免重复购买 #生活 ^2lvo
  - [ ] 规划去菜市场或超市的路线 #生活 ^6gnl
  - [ ] 备好购物袋和支付方式出门采购 #生活 ^cxdx

- [ ] 准备下周答辩 #工作 ^p1a2
  - [x] 整理实验数据成图表 ^c001
  - [ ] 写演讲稿逐字稿 🗓️2026-07-16 ^c002
  - [ ] 做 PPT 🗓️2026-07-17 ^c003

- [ ] 交周报 #工作 ^b7c1
  - [ ] 整理本周已完成工作要点 ^eiv7
  - [ ] 汇总关键数据和进度指标 ^6444
  - [ ] 撰写周报正文内容 ^4dy1
  - [ ] 检查排版并发送给主管 ^fxnx
